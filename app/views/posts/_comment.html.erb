<% post.comments.each do |comment| %>
  <div class='comment'>
    <p><strong><%= comment.author.username %> says:</strong>
      (<%= comment.created_at %>)</p>
    <p><%= comment.content %></p>

  </div>
<% end %>